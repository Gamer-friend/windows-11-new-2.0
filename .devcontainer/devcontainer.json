{
  "name": "Windows VM in Docker",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "chmod +x /usr/local/bin/start && echo '✅ Environment ready. Run ./start to launch Windows VM.'",
  "customizations": {
    "codespaces": {
      "openFiles": ["README.md"]
    }
  }
}
